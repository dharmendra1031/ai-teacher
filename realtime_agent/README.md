# Realtime agent placeholder

The production realtime agent remains a separate Python 3.12 service. Phase 6 adds the LiveKit Agents lifecycle and model adapters. Phase 2 only locks the runtime boundary and verifies the container.

