name: ai_teacher_phase1_livekit
publish_to: "none"
description: Temporary Phase 1 Flutter LiveKit feasibility client.
version: 0.1.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: 4.7.3
  http: 1.6.0
  livekit_client: 2.8.1
  permission_handler: 12.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
